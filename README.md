Demonstrates how to build and deploy a dependecy Python library to AWS EMR serverless spark.

Run `deploy_library_to_aws.ps1` in the `spark` directory.

copy  `serverless_demo.ipynb` to the EMR Studio Workspace.  Replce the `{S3_bucket}` in the conf json with your value.

